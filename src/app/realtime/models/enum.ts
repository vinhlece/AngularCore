export enum ColorStyle {
  Solid = 0, // current
  Slash = 1, // go back
  Dash = 2 // timestamp
}

export enum TreeExpression {
  $lte = '$lte', // less than OR equal
  $eq = '$eq' // equal
}

export enum ConvertMode {
  Combine = 'Combine',
  Kpi = 'Kpi',
  All = 'All'
}

