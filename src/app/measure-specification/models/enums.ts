export enum MeasureMetricTypes {
  Count = 'COUNTER',
  Duration = 'DURATION',
  Timer = 'TIMER'
}

export enum DurationType {
  Increment = 'INC',
  Decrement = 'DEC',
}

export enum EditingEventTag {
  OnCustom = 'customEventTags',
  OnEvent = 'eventTags'
}
